# SimHGS: low uncertainty

`SimHGSLowSol.zip` contains SimHGS solution and simulation files under arrival deviations `Tri(-3, 3, 0)`. These outputs are compared with low-uncertainty baseline policies.
