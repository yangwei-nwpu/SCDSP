# Scenario size 100

`SampleSize100.zip` contains neighborhood move-selection statistics generated with 100 in-search scenarios. The `*_SamSta.txt` files are compared with the 2,000-scenario reference evaluation in `../../SampleSizeRobust.ipynb`.
